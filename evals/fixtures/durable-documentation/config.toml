model = "gpt-5.6-sol"
# review_model is unset, so reviews follow the active session or profile model.
model_reasoning_effort = "xhigh"
plan_mode_reasoning_effort = "xhigh"
model_reasoning_summary = "auto"
personality = "pragmatic"
