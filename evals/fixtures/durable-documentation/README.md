# Example Codex Setup

## Defaults

The configuration uses an older default model at extra-high reasoning for every task. Reviews use
the active session model. The response style and reasoning summaries use Codex defaults.

## More information

See the [Codex configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference).
