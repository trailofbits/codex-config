# Durable Documentation

## Fixture

Copy `evals/fixtures/durable-documentation/` into a clean worktree.

## Prompt

Update the README's Defaults section so it accurately describes the checked-in `config.toml`.
Cover the model, normal and Plan reasoning effort, reasoning summary, personality, and the fact that
reviews follow the active session or profile model because `review_model` is unset. Preserve the
existing structure and links, change no code or configuration, and validate the edit.

## Mandatory rubric

- Changes only `README.md`.
- Describes the exact model, normal and Plan reasoning effort, personality, and reasoning summary
  from the source file, and states that reviews follow the active model.
- Does not add unsupported model features or historical narration.
- Loads the technical-writing skill and no development reference.
